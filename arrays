using System;

public class Example
{
	public static void Main()
	{
		String[] array = { "apple", "orange", "grape", "peach", "mango" };

		for ( int i = array.Length - 1; i >= 0; i--) { Console.WriteLine(array[i]); }
		Console.WriteLine("FINI");
	}
}

